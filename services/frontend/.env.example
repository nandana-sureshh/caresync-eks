# CareSync Frontend — Environment Variables
# Copy this file to .env and fill in your values before running locally.
# For Docker/EC2: these are injected via docker-compose build.args at build time.

REACT_APP_AUTH_URL=
REACT_APP_PATIENT_URL=
REACT_APP_DOCTOR_URL=
REACT_APP_APPOINTMENT_URL=
